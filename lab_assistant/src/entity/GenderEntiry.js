/**
 * 性別
 */
export class Gender {
  /**
   * コンストラクタ
   * @param {Number} id ID
   * @param {String} name 名前
   */
  constructor({
    id, name,
  }) {
    this.id = id;
    this.name = name;
  }
}
